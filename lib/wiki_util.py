"""
This module is for processing wiki data.

methods:
    - extract_media_refs: extract media references from wiki data
    - extract_basic_info: extract basic info from wiki data
    - remove_emphasis: remove emphasis from wiki data
    - remove_link: remove link from wiki data
    - remove_markup: remove markup from wiki data
"""

import re


def extract_media_refs(text: str) -> list:
    """
    extract media references from wiki data
    args:
        text (str): wiki text
    return:
        media_refs (list): media references
    """
    pass


def extract_basic_info(text: str) -> str:
    """
    extract basic info from wiki data
    args:
        text (str): wiki text
    return:
        basic_info (str): basic info
    """
    pass


def remove_emphasis(text: str) -> str:
    """
    remove emphasis from wiki data
    args:
        text (str): wiki data
    return:
        text (str): wiki data without emphasis
    """
    pass


def remove_link(text: str) -> str:
    """
    remove link from wiki data
    args:
        text (str): wiki data
    return:
        text (str): wiki data without link
    """
    pass


def remove_markup(text: str) -> str:
    """
    remove markup from wiki data
    args:
        text (str): wiki data
    return:
        text (str): wiki data without markup
    """
    pass